请继续开发 VisualTeX Next 项目。项目原目录为 /Users/lpj/devspace/workspaces/visualstudio，迁移后请先打开压缩包中的 visualstudio 项目根目录，并完整阅读 PROJECT_ARCHITECTURE_AND_IMPLEMENTATION_PLAN.md、README.md、docs/IMPLEMENTATION_STATUS.md、docs/PROTOCOL.md、docs/SECURITY.md 和 scripts/texstudio/README.md。该项目是由 Rust、Tauri、React、TypeScript、Python 组成的多工作区工程，始终以 LaTeX 源码作为唯一权威数据源；目前已经完成 revisioned UTF-8 Rope 缓冲、原子保存、统一 Undo/Redo、恢复日志、外部文件监听与冲突副本、多文件项目索引、搜索替换与安全重命名、标准项目模板、Tree-sitter LaTeX 全量与增量语法树、错误/缺失节点、verbatim/minted 保护范围、input/include/subfile 依赖图与循环检测、容错 LaTeX 语义节点、CodeMirror 源码编辑、ProseMirror 结构化编辑、MathLive 公式编辑、latexmk/XeLaTeX/pdfLaTeX/LuaLaTeX/Tectonic 本地编译、结构化诊断、PDFium 页面渲染与缓存、SyncTeX 正反向定位、PDF 页面节点映射与覆盖编辑、离线 OCR Worker、本地模型包 manifest 与 SHA-256 校验、暂存后原子安装、活动模型持久化、公式 OCR 候选校对与统一撤销写回、PP-StructureV3 整页 OCR 区域识别、阅读顺序、低置信度提示、区域类型修改、公式校对、LaTeX 草稿和 OCR 新项目生成；VS Code 扩展已经实现 CustomTextEditorProvider、TextDocument 权威、WorkspaceEdit 写回、Core 自动重连、精确保存确认、PDFium 页面、双向 SyncTeX、公式 OCR 和整页 OCR；TeXstudio 与通用 Bridge 已实现 127.0.0.1 随机端口、项目独立令牌、协议版本协商、1 MiB 消息限制、重复服务拒绝、多项目并行、断线恢复、非法令牌与版本拒绝、外部编辑器双向文件同步及冲突检测，并提供 POSIX 和 Windows PowerShell 适配脚本；还完成了 Node Adapter SDK、Rust vt-uri、版本化 visualtex:// URI、Tauri deep-link 注册以及 CLI 的 open、open-uri、bridge、model、OCR、compile 和 SyncTeX 命令。已通过的分项验证包括 Tree-sitter 语法层 6 项测试、Core 14 项测试、RPC 依赖图能力测试、Bridge 6 项测试、Adapter SDK 5 项测试、VS Code 扩展 12 项测试、桌面前端 6 项测试、桌面 Rust 测试、URI 2 项测试、OCR Worker、模型包与图片安全测试、Tauri debug build、Windows x64 NSIS 安装包构建与静默安装/卸载冒烟测试、中文和空格路径下的 TeXstudio 真实 XeLaTeX 编译与双向 SyncTeX、visualtex:// CLI 真实测试以及相关模块严格 Clippy。迁移后先确认 Rust 1.96、Node.js 24、Corepack、pnpm 10、Python 3.9 以上版本和本地 TeX Live/latexmk；在项目根目录运行 corepack pnpm install，然后依次运行 cargo fmt --all -- --check、cargo clippy --workspace --all-targets -- -D warnings、cargo test --workspace、corepack pnpm -r typecheck、corepack pnpm -r test、corepack pnpm -r build 和 corepack pnpm --filter @visualtex/desktop tauri build --debug --no-bundle，最后运行 cargo run -p visualtex-cli -- compile fixtures/templates/basic-zh 做真实中文 LaTeX 编译。必须重点检查本轮文档、CI、deep-link、Bridge、vt-uri 和 Adapter SDK 合并后的最终全工作区门禁，因为原开发会话在最后聚合门禁前发生连接中断；Windows PowerShell 适配脚本现已在 Windows 10、PowerShell 5.1、TeXstudio 4.8.9 和 TeX Live 2025 环境完成实机运行验证，包括安装幂等与覆盖拒绝、中文和空格路径下的 Bridge 自动启动、真实 XeLaTeX 编译、双向 SyncTeX 与正常关闭；TeXstudio 版本专用占位符和 GUI 宏配置仍未完成自动化宿主测试。项目原来没有独立 .git，并且被上层 /Users/lpj/devspace 仓库 .git/info/exclude 中的 workspaces/ 规则忽略，迁移后应在 visualstudio 根目录单独执行 git init，创建 dev 分支并首次提交，不要误认为压缩包包含已有 Git 历史。生产 OCR 模型不会包含在源码迁移包中，需要另外安装符合 visualtex-model.json 的本地公式模型和 layout_ocr 配置包。仍未完成的主要工作包括更完整的 import/package/class/宏依赖图、源文件删除与重命名生命周期、更大的真实语料与专用 fuzz、更加明确的性能阈值、更强的 NodeId 连续性、富剪贴板和 IME/e2e 压力测试、完整 texlab/LSP、Git、拼写检查和格式化工作台、随包生产 OCR 模型与正式基准、插件 manifest、权限体系和 WASI 沙箱、macOS/Linux 正式安装包、Windows Authenticode 签名、macOS 公证、自动更新验证、i18n、无障碍检查以及完整的性能、泄漏和 PDF 视觉回归矩阵。继续开发时不要删除 Cargo.lock、pnpm-lock.yaml、apps、crates、packages、services/ocr-python、fixtures、scripts、docs 和 .github；不要提交 target、node_modules、dist、.DS_Store、.visualtex/build、.visualtex/cache、.visualtex/recovery 或 Bridge 会话文件。请先执行全部门禁并修复失败，再根据 docs/IMPLEMENTATION_STATUS.md 从未完成阶段继续，不得虚报全部阶段已经完成。