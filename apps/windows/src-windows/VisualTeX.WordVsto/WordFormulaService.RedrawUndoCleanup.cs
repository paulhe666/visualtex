namespace VisualTeX.WordVsto;

// Intentionally empty. OMML/VisualTeX mutations now own one explicit Word
// Custom UndoRecord and do not create deferred metadata cleanup tickets.
internal sealed partial class WordFormulaService
{
}
