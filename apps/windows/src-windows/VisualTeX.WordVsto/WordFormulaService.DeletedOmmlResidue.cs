namespace VisualTeX.WordVsto;

// Retained as an empty partial source file so existing project/acceptance file
// references do not reintroduce the retired "deleted OMML residue" repair model.
// The rebuilt host core derives existence from current Word hosts only.
internal sealed partial class WordFormulaService
{
}
