import { randomUUID } from "node:crypto";
import type * as vscode from "vscode";

export function createWebviewHtml(webview: vscode.Webview): string {
  const nonce = randomUUID().replaceAll("-", "");
  return `<!doctype html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<meta http-equiv="Content-Security-Policy" content="default-src 'none'; img-src ${webview.cspSource} data:; style-src 'unsafe-inline'; script-src 'nonce-${nonce}';">
<style>
:root{color-scheme:light dark}*{box-sizing:border-box}body{margin:0;font-family:var(--vscode-font-family);color:var(--vscode-foreground);background:var(--vscode-editor-background)}header.app{position:sticky;top:0;z-index:8;display:flex;align-items:center;gap:8px;padding:8px 10px;border-bottom:1px solid var(--vscode-panel-border);background:var(--vscode-editor-background)}button,select,textarea{font:inherit}button{color:var(--vscode-button-foreground);background:var(--vscode-button-background);border:0;border-radius:4px;padding:5px 9px;cursor:pointer}button.secondary{color:var(--vscode-foreground);background:var(--vscode-button-secondaryBackground)}button:disabled{opacity:.4;cursor:default}.status{margin-left:auto;color:var(--vscode-descriptionForeground);font-size:11px}.tabs{display:flex;gap:3px}.tabs button.active{outline:2px solid var(--vscode-focusBorder)}main{min-height:calc(100vh - 45px)}.panel{display:none}.panel.active{display:block}.editor-panel{padding:0 14px 32px}.node{border:1px solid var(--vscode-panel-border);border-radius:6px;margin:10px 0;overflow:hidden}.label{font-size:10px;text-transform:uppercase;color:var(--vscode-descriptionForeground);background:var(--vscode-sideBar-background);padding:5px 8px}.node textarea{display:block;width:100%;min-height:56px;resize:vertical;padding:9px;border:0;outline:0;color:var(--vscode-input-foreground);background:var(--vscode-input-background);line-height:1.55}.node[data-kind=title] textarea{font-size:1.5em;font-weight:700;text-align:center}.node[data-kind=section] textarea{font-size:1.2em;font-weight:700}.readonly{opacity:.7}.diagnostics{padding:4px 14px 12px}.diagnostic{margin:5px 0;color:var(--vscode-errorForeground);font-size:11px}.pdf-panel{height:calc(100vh - 46px);grid-template-rows:auto minmax(0,1fr)}.pdf-panel.active{display:grid}.pdf-toolbar{display:flex;align-items:center;justify-content:center;gap:6px;padding:6px;border-bottom:1px solid var(--vscode-panel-border)}.pdf-scroll{min-height:0;overflow:auto;padding:20px;background:var(--vscode-sideBar-background)}.pdf-page{position:relative;width:min(100%,1000px);margin:0 auto;background:white;box-shadow:0 4px 18px rgba(0,0,0,.25)}.pdf-page img{display:block;width:100%;height:auto;user-select:none}.highlight-layer{position:absolute;inset:0;pointer-events:none}.highlight{position:absolute;border:1px solid #f0a000;background:rgba(255,203,57,.32)}.empty{padding:40px;text-align:center;color:var(--vscode-descriptionForeground)}.error-line{padding:6px 10px;color:var(--vscode-errorForeground);border-bottom:1px solid var(--vscode-panel-border);font-size:11px}.ocr-panel{padding:12px 14px 30px}.ocr-toolbar{display:flex;align-items:center;gap:7px;flex-wrap:wrap;padding-bottom:10px;border-bottom:1px solid var(--vscode-panel-border)}.ocr-health{margin-left:auto;color:var(--vscode-descriptionForeground);font-size:10px}.ocr-status{padding:8px 0;color:var(--vscode-descriptionForeground);font-size:11px}.ocr-review{margin-top:12px;border:1px solid var(--vscode-panel-border);border-radius:8px;overflow:hidden}.ocr-review[hidden]{display:none}.ocr-review>header{padding:8px 10px;background:var(--vscode-sideBar-background);border-bottom:1px solid var(--vscode-panel-border);font-size:11px;font-weight:700}.formula-grid{display:grid;grid-template-columns:minmax(180px,.8fr) minmax(260px,1.2fr);gap:12px;padding:12px}.formula-grid img{max-width:100%;max-height:300px;margin:auto;background:#fff}.formula-controls{display:grid;gap:8px;align-content:start}.formula-controls textarea{width:100%;min-height:120px;resize:vertical;padding:8px;color:var(--vscode-input-foreground);background:var(--vscode-input-background);border:1px solid var(--vscode-input-border);border-radius:4px;font-family:var(--vscode-editor-font-family)}.formula-controls select{padding:5px;color:var(--vscode-dropdown-foreground);background:var(--vscode-dropdown-background);border:1px solid var(--vscode-dropdown-border)}.formula-actions{display:flex;justify-content:flex-end;gap:6px}.warning-list{margin:0;padding:0 12px 10px;color:var(--vscode-editorWarning-foreground);font-size:10px}.document-grid{display:grid;grid-template-columns:minmax(280px,1fr) minmax(320px,1fr);height:min(720px,calc(100vh - 190px));min-height:430px}.document-image-scroll,.region-list{min-height:0;overflow:auto;padding:12px;background:var(--vscode-sideBar-background)}.document-image-wrap{position:relative;width:100%;background:#fff;box-shadow:0 3px 14px rgba(0,0,0,.24)}.document-image-wrap img{display:block;width:100%;height:auto}.region-overlay{position:absolute;border:1px solid #2b72ff;background:rgba(43,114,255,.10);padding:0;color:#fff}.region-overlay span{position:absolute;left:0;top:0;transform:translateY(-100%);padding:1px 3px;background:#2b72ff;font-size:8px}.region-overlay.low{border-color:#d47b00;background:rgba(235,150,35,.18)}.region-overlay.low span{background:#b56600}.region-overlay.ignored{border-style:dashed;border-color:#777;background:rgba(100,100,100,.08);opacity:.65}.region-list{background:var(--vscode-editor-background);border-left:1px solid var(--vscode-panel-border)}.region-card{margin-bottom:8px;border:1px solid var(--vscode-panel-border);border-radius:6px;overflow:hidden}.region-card.low{border-color:#d47b00}.region-head{display:grid;grid-template-columns:minmax(70px,1fr) minmax(90px,auto) auto;gap:5px;align-items:center;padding:5px;background:var(--vscode-sideBar-background)}.region-head strong{font-size:10px}.region-head select{min-width:0;padding:3px;color:var(--vscode-dropdown-foreground);background:var(--vscode-dropdown-background);border:1px solid var(--vscode-dropdown-border);font-size:10px}.order-buttons{display:flex;gap:3px}.order-buttons button{padding:2px 6px}.region-card textarea{display:block;width:100%;min-height:72px;resize:vertical;padding:7px;border:0;outline:0;color:var(--vscode-input-foreground);background:var(--vscode-input-background);font-family:var(--vscode-editor-font-family);font-size:11px}.region-card .ignored-message{padding:12px;color:var(--vscode-descriptionForeground);font-size:10px;font-style:italic}.document-actions{display:flex;justify-content:flex-end;gap:7px;padding:9px 10px;border-top:1px solid var(--vscode-panel-border)}@media(max-width:760px){.formula-grid,.document-grid{grid-template-columns:1fr}.document-grid{height:auto}.document-image-scroll,.region-list{max-height:500px}.region-list{border-left:0;border-top:1px solid var(--vscode-panel-border)}}
</style>
</head>
<body>
<header class="app"><strong>VisualTeX Next</strong><div class="tabs"><button id="editorTab" class="secondary active">结构</button><button id="pdfTab" class="secondary">PDF</button><button id="ocrTab" class="secondary">OCR</button></div><button id="compile">编译</button><button id="reconnect" class="secondary">重连 Core</button><span class="status" id="status"></span></header>
<div id="error"></div>
<main>
<section id="editorPanel" class="panel editor-panel active"><div id="nodes"></div><div id="diagnostics" class="diagnostics"></div></section>
<section id="pdfPanel" class="panel pdf-panel"><div class="pdf-toolbar"><button id="previous" class="secondary">上一页</button><span id="pageLabel">0 / 0</span><button id="next" class="secondary">下一页</button><select id="zoom"><option value="0.8">80%</option><option value="1" selected>100%</option><option value="1.3">130%</option><option value="1.7">170%</option></select></div><div class="pdf-scroll" id="pdfScroll"><div id="pdfEmpty" class="empty">编译成功后显示 PDFium 渲染页面。</div><div id="pdfPage" class="pdf-page" hidden><img id="pdfImage" alt="Rendered PDF page"><div id="highlightLayer" class="highlight-layer"></div></div></div></section>
<section id="ocrPanel" class="panel ocr-panel"><div class="ocr-toolbar"><button id="formulaOcr">公式图片 OCR</button><button id="documentOcr">整页 OCR</button><button id="refreshOcr" class="secondary">检查模型</button><span id="ocrHealth" class="ocr-health">尚未检查模型</span></div><div id="ocrStatus" class="ocr-status">图片必须位于当前受信任工作区；识别全程离线。</div><section id="formulaReview" class="ocr-review" hidden><header>公式 OCR 校对</header><div class="formula-grid"><img id="formulaImage" alt="Formula OCR source"><div class="formula-controls"><select id="formulaCandidate"></select><textarea id="formulaLatex" spellcheck="false"></textarea><select id="formulaMode"><option value="inline">行内公式 $…$</option><option value="display">陈列公式 \\[ … \\]</option><option value="raw">仅插入 LaTeX</option></select><div class="formula-actions"><button id="insertFormula">写入源码选择位置</button></div></div></div><div id="formulaWarnings" class="warning-list"></div></section><section id="documentReview" class="ocr-review" hidden><header>整页 OCR 校对：调整类型、顺序与内容后再写入</header><div class="document-grid"><div class="document-image-scroll"><div class="document-image-wrap" id="documentImageWrap"><img id="documentImage" alt="Document OCR source"><div id="documentOverlay"></div></div></div><div id="regionList" class="region-list"></div></div><div class="document-actions"><span id="documentSummary" class="ocr-health"></span><button id="insertDocument">按校对后阅读顺序写入源码</button></div><div id="documentWarnings" class="warning-list"></div></section></section>
</main>
<script nonce="${nonce}">
const vscode=acquireVsCodeApi();const byId=id=>document.getElementById(id);const nodes=byId('nodes');const status=byId('status');const diagnostics=byId('diagnostics');const errorBox=byId('error');const panels={editor:byId('editorPanel'),pdf:byId('pdfPanel'),ocr:byId('ocrPanel')};const tabs={editor:byId('editorTab'),pdf:byId('pdfTab'),ocr:byId('ocrTab')};const pdfPage=byId('pdfPage');const pdfImage=byId('pdfImage');const pdfEmpty=byId('pdfEmpty');const pageLabel=byId('pageLabel');const previous=byId('previous');const next=byId('next');const zoom=byId('zoom');const highlightLayer=byId('highlightLayer');const pdfScroll=byId('pdfScroll');const formulaReview=byId('formulaReview');const formulaImage=byId('formulaImage');const formulaCandidate=byId('formulaCandidate');const formulaLatex=byId('formulaLatex');const formulaMode=byId('formulaMode');const formulaWarnings=byId('formulaWarnings');const documentReview=byId('documentReview');const documentImage=byId('documentImage');const documentOverlay=byId('documentOverlay');const regionList=byId('regionList');const documentSummary=byId('documentSummary');const documentWarnings=byId('documentWarnings');const ocrStatus=byId('ocrStatus');const ocrHealth=byId('ocrHealth');let pdfInfo=null;let currentPage=0;let highlights=[];let formulaResult=null;let documentResult=null;
const regionKinds=[['text','正文'],['paragraph_title','小标题'],['document_title','文档标题'],['abstract','摘要'],['formula','公式'],['table','表格'],['figure','图片/图表'],['list','列表'],['reference','参考文献'],['header','页眉'],['footer','页脚'],['page_number','页码'],['ignore','忽略']];
function setTab(tab){for(const key of Object.keys(panels)){panels[key].classList.toggle('active',key===tab);tabs[key].classList.toggle('active',key===tab)}if(tab==='pdf'&&pdfInfo)requestPage(currentPage)}
function render(snapshot){status.textContent=snapshot.path+' · rev '+snapshot.revision+(snapshot.dirty?' · dirty':'');nodes.replaceChildren();for(const node of snapshot.nodes){if(node.kind==='document'||node.text===null)continue;const box=document.createElement('section');box.className='node'+(node.support==='opaque'||node.support==='unstable'?' readonly':'');box.dataset.kind=node.kind;const label=document.createElement('div');label.className='label';label.textContent=node.kind+' · '+node.support;const editor=document.createElement('textarea');editor.value=node.text;editor.disabled=node.support==='opaque'||node.support==='unstable';editor.addEventListener('change',()=>vscode.postMessage({type:'visualEdit',nodeId:node.id,content:editor.value}));box.append(label,editor);nodes.append(box)}}
function renderDiagnostics(items){diagnostics.replaceChildren();for(const item of items||[]){const row=document.createElement('div');row.className='diagnostic';row.textContent=(item.file?item.file+':':'')+(item.line||'')+' '+item.message;diagnostics.append(row)}}
function renderPdfState(){const pages=pdfInfo?.pages?.length||0;pageLabel.textContent=pages?String(currentPage+1)+' / '+String(pages):'0 / 0';previous.disabled=currentPage<=0;next.disabled=!pages||currentPage>=pages-1;pdfEmpty.hidden=pages>0;pdfPage.hidden=pages===0}
function requestPage(page){if(!pdfInfo)return;currentPage=Math.max(0,Math.min(pdfInfo.pages.length-1,page));renderPdfState();const width=Math.max(640,Math.min(3000,Math.round(pdfScroll.clientWidth*devicePixelRatio*Number(zoom.value))));vscode.postMessage({type:'renderPdfPage',pageIndex:currentPage,width})}
function renderHighlights(){highlightLayer.replaceChildren();if(!pdfInfo)return;const page=pdfInfo.pages[currentPage];if(!page)return;for(const box of highlights){if(box.page!==currentPage+1)continue;const item=document.createElement('span');item.className='highlight';item.style.left=(box.x/page.widthPoints*100)+'%';item.style.top=(box.y/page.heightPoints*100)+'%';item.style.width=(box.width/page.widthPoints*100)+'%';item.style.height=(box.height/page.heightPoints*100)+'%';highlightLayer.append(item)}}
function renderFormula(message){formulaResult=message.result;formulaImage.src=message.imageUri;formulaCandidate.replaceChildren();for(let index=0;index<formulaResult.candidates.length;index++){const candidate=formulaResult.candidates[index];const option=document.createElement('option');option.value=String(index);option.textContent=Math.round(candidate.confidence*100)+'% · '+candidate.latex;formulaCandidate.append(option)}formulaLatex.value=formulaResult.candidates[0]?.latex||'';formulaWarnings.textContent=(formulaResult.warnings||[]).join(' · ');formulaReview.hidden=false;documentReview.hidden=true;setTab('ocr');ocrStatus.textContent='公式识别完成，请校对后写入。'}
function contentOf(region){return region.latex??region.text??''}function setContent(region,value){if(region.kind.toLowerCase().includes('formula')){region.latex=value;region.text=null}else{region.text=value;region.latex=null}}function changeKind(region,kind){const content=contentOf(region);region.kind=kind;setContent(region,content)}
function moveRegion(regionIndex,delta){const position=documentResult.readingOrder.indexOf(regionIndex);const target=position+delta;if(position<0||target<0||target>=documentResult.readingOrder.length)return;const order=documentResult.readingOrder;[order[position],order[target]]=[order[target],order[position]];renderDocumentRegions()}
function renderDocument(message){documentResult=structuredClone(message.result);documentImage.src=message.imageUri;formulaReview.hidden=true;documentReview.hidden=false;documentWarnings.textContent=(documentResult.warnings||[]).join(' · ');renderDocumentRegions();setTab('ocr');ocrStatus.textContent='整页识别完成：橙色区域置信度较低，请优先校对。'}
function renderDocumentRegions(){documentOverlay.replaceChildren();regionList.replaceChildren();let low=0;for(let order=0;order<documentResult.readingOrder.length;order++){const regionIndex=documentResult.readingOrder[order];const region=documentResult.regions[regionIndex];if(!region)continue;const ignored=region.kind.toLowerCase()==='ignore';const isLow=!ignored&&region.confidence<.65;if(isLow)low++;const box=document.createElement('button');box.className='region-overlay'+(isLow?' low':'')+(ignored?' ignored':'');box.style.left=(region.x/Math.max(1,documentResult.pageWidth)*100)+'%';box.style.top=(region.y/Math.max(1,documentResult.pageHeight)*100)+'%';box.style.width=(region.width/Math.max(1,documentResult.pageWidth)*100)+'%';box.style.height=(region.height/Math.max(1,documentResult.pageHeight)*100)+'%';const number=document.createElement('span');number.textContent=String(order+1);box.append(number);box.addEventListener('click',()=>byId('ocr-region-'+regionIndex)?.scrollIntoView({behavior:'smooth',block:'center'}));documentOverlay.append(box);const card=document.createElement('article');card.id='ocr-region-'+regionIndex;card.className='region-card'+(isLow?' low':'');const head=document.createElement('div');head.className='region-head';const title=document.createElement('strong');title.textContent=(order+1)+'. 区域 '+(regionIndex+1)+' · '+Math.round(region.confidence*100)+'%';const select=document.createElement('select');const normalized=region.kind.toLowerCase();if(!regionKinds.some(item=>item[0]===normalized)){const custom=document.createElement('option');custom.value=normalized;custom.textContent=region.kind;select.append(custom)}for(const [value,label] of regionKinds){const option=document.createElement('option');option.value=value;option.textContent=label;select.append(option)}select.value=normalized;select.addEventListener('change',()=>{changeKind(region,select.value);renderDocumentRegions()});const orderButtons=document.createElement('div');orderButtons.className='order-buttons';const up=document.createElement('button');up.className='secondary';up.textContent='↑';up.disabled=order===0;up.addEventListener('click',()=>moveRegion(regionIndex,-1));const down=document.createElement('button');down.className='secondary';down.textContent='↓';down.disabled=order===documentResult.readingOrder.length-1;down.addEventListener('click',()=>moveRegion(regionIndex,1));orderButtons.append(up,down);head.append(title,select,orderButtons);card.append(head);if(ignored){const note=document.createElement('div');note.className='ignored-message';note.textContent='该区域不会写入 LaTeX。';card.append(note)}else{const editor=document.createElement('textarea');editor.value=contentOf(region);editor.rows=region.kind.toLowerCase().includes('table')?7:3;editor.spellcheck=false;editor.addEventListener('input',()=>setContent(region,editor.value));card.append(editor)}regionList.append(card)}documentSummary.textContent=documentResult.regions.length+' 个区域 · '+low+' 个低置信度'}
window.addEventListener('message',event=>{const message=event.data;if(message.type==='snapshot')render(message.snapshot);else if(message.type==='coreStatus'){const state=message.status.state;status.textContent=state==='connected'?'Core 已连接':state==='connecting'?'Core 连接中…':'Core '+state+(message.status.detail?' · '+message.status.detail:'')}else if(message.type==='compileResult'){renderDiagnostics(message.artifact.diagnostics);pdfInfo=message.pdfInfo;currentPage=0;renderPdfState();if(pdfInfo)setTab('pdf')}else if(message.type==='pdfPage'){currentPage=message.pageIndex;pdfImage.src=message.imageUri;renderPdfState();renderHighlights()}else if(message.type==='pdfHighlights'){highlights=message.boxes||[];renderHighlights()}else if(message.type==='ocrStatus'){ocrStatus.textContent=message.message;setTab('ocr')}else if(message.type==='ocrHealth'){ocrHealth.textContent=message.health?('公式 '+(message.health.formula.available?'可用':'未配置')+' · 整页 '+(message.health.document.available?'可用':'未配置')):('模型检查失败 · '+(message.error||''))}else if(message.type==='formulaOcrResult')renderFormula(message);else if(message.type==='documentOcrResult')renderDocument(message);else if(message.type==='operationError'){errorBox.className='error-line';errorBox.textContent=message.message}});
pdfImage.addEventListener('dblclick',event=>{if(!pdfInfo)return;const page=pdfInfo.pages[currentPage];const bounds=pdfImage.getBoundingClientRect();vscode.postMessage({type:'inverseSearch',page:currentPage+1,x:(event.clientX-bounds.left)/bounds.width*page.widthPoints,y:(event.clientY-bounds.top)/bounds.height*page.heightPoints})});formulaCandidate.addEventListener('change',()=>{formulaLatex.value=formulaResult?.candidates[Number(formulaCandidate.value)]?.latex||''});byId('insertFormula').addEventListener('click',()=>vscode.postMessage({type:'insertFormulaOcr',latex:formulaLatex.value,mode:formulaMode.value}));byId('insertDocument').addEventListener('click',()=>vscode.postMessage({type:'insertDocumentOcr',result:documentResult}));byId('formulaOcr').addEventListener('click',()=>vscode.postMessage({type:'chooseFormulaOcr'}));byId('documentOcr').addEventListener('click',()=>vscode.postMessage({type:'chooseDocumentOcr'}));byId('refreshOcr').addEventListener('click',()=>vscode.postMessage({type:'refreshOcrHealth'}));tabs.editor.addEventListener('click',()=>setTab('editor'));tabs.pdf.addEventListener('click',()=>setTab('pdf'));tabs.ocr.addEventListener('click',()=>setTab('ocr'));byId('compile').addEventListener('click',()=>vscode.postMessage({type:'compile'}));byId('reconnect').addEventListener('click',()=>vscode.postMessage({type:'reconnect'}));previous.addEventListener('click',()=>requestPage(currentPage-1));next.addEventListener('click',()=>requestPage(currentPage+1));zoom.addEventListener('change',()=>requestPage(currentPage));let resizeTimer;window.addEventListener('resize',()=>{clearTimeout(resizeTimer);resizeTimer=setTimeout(()=>{if(panels.pdf.classList.contains('active')&&pdfInfo)requestPage(currentPage)},180)});vscode.postMessage({type:'ready'});
</script></body></html>`;
}
