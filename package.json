{
  "name": "visualtex-platform-repository",
  "private": true,
  "version": "1.2.1",
  "license": "MIT",
  "description": "Platform-separated VisualTeX desktop applications for macOS and Windows.",
  "scripts": {
    "bootstrap:macos": "npm --prefix apps/macos ci",
    "bootstrap:windows": "npm --prefix apps/windows ci",
    "bootstrap": "npm run bootstrap:macos && npm run bootstrap:windows",
    "build:macos": "npm --prefix apps/macos run build:desktop",
    "build:windows": "npm --prefix apps/windows run build:desktop",
    "build": "npm run build:macos && npm run build:windows",
    "test:macos": "npm --prefix apps/macos run test:macos-offline-office",
    "test:windows": "npm --prefix apps/windows run test:windows-office-architecture",
    "test:repository": "node tools/verify_repository_structure.mjs",
    "check": "npm run test:repository && npm run build"
  }
}
