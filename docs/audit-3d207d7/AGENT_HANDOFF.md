# 给实现 agent 的交接

先完整阅读同目录 `AUDIT_REPORT.md`，不要只读本页或沿用对话早期未经查证的推测。报告中的源码位置、真实 Word 用例、正常对照和证据索引是本次整改依据。

## 工作区和保护要求

工作区为 `C:\Users\pojian_liao\Desktop\devspace\visualtex1.2.3-reference-3d207d7`，主要项目 `apps\windows`，基线 HEAD 为 `3d207d7a81ff40014fd861b242cff96ea77060a2`。审查没有修改产品实现。`Cargo.toml` 的既有状态标记没有逻辑差异；新增审查文件均在本目录。不得 reset/clean/stash、切分支、删除未知文件、擅自提交、push 或发布。

用户原始文档1、22、26、28、29、33、35、36仍打开且未保存，不得在这些原件上做修复试验，也不得关闭或替用户保存。通过真实 Word 新建测试文档复现和验收。现有复现文档与会话编号见报告第4节。不要强杀 Word 进程，因为原件和测试文档在同一进程。

## 已确认的公共问题

- 转换生产 N×3 OMML 宿主，编辑消费者仍强制 1×3 并使用固定行；失败恢复不能恢复完整公式、编号和书签。更新及直接关闭均留下重复公式与缺失编号。
- 前方公式结构变更使邻接非目标公式的身份书签扩张或目标宿主错配。非空边界和指纹异常是已发现的后果，不是可以关掉的保护。首次失效 COM 步骤需在实施时进一步跟踪。
- 批量插入延期物化编号，全局“健康”快速路径却只检查旧编号产物，不核对新增 Numbered=true 公式集合；因此已有编号时漏建新编号，空文档可以通过。
- MathType 转换主动保留 VTEqNum 引用别名，编号重写却只接受 ZEqnNum，拒绝自身生成的兼容状态。
- 批量 OMML 临时 Word 文档漏传数学字体，默认 Cambria Math，与直接插入的 Latin Modern Math 分叉。
- 重绘复用新建插入流程，未完整隔离公式前后正文；手动换行触发 OMML 无编号、MathType 公式集中到正文后、VT OLE 污染已有正文格式。生产编号协调还存在吞异常行为。
- 原始 cases 的真实渲染输出经 MTEF 往返，语义签名仅相差空右定界符的 `o()`；不是已证明分式或微分命令无法转译。应规范化单侧围栏语义并验证几何，不得关闭整个校验。

## 实施和完成要求

先修复宿主与身份契约、事务恢复，再修编号物化/增量刷新和引用别名生命周期；随后统一字体上下文、重绘范围及正文格式保护、空定界符规范化。以小步可审查修改推进，收敛互相矛盾的旧分支，不针对某个文档名、GUID 或某段 LaTeX 写特解。

每步必须通过真实已安装插件的 Ribbon/对应回调、编辑器、导入和渲染链验证；不要求模拟鼠标坐标，但不能直接写入最终 XML、修改 session.json 或创建测试专用服务实例后宣称与用户操作等价。使用临时文档或纯数据测试只作补充。不得用 MathType 的 VBA/宏或安装依赖绕开 VisualTeX 自主实现。

不得删引用书签、强删非空边界、放宽所有语义差异、禁用指纹、吞异常或取消用户已选编号来“通过”测试。每条公式的内容、ID、宿主、编号、引用要一一对应，非目标正文和对象必须不变。失败恢复必须验证完整结构，而不是只比较公式数量。

必须跑完报告第14节矩阵及此前正常对照，并覆盖保存重开、再次编辑/转换、多次操作、已存在编号与混合正文场景。用户提到的“编号更新但引用未更新”尚未被干净隔离为独立变体，不能在关闭更早的编号重写错误后漏测它。性能优化必须保留正确性前置条件，不能把每次局部编辑改为全篇慢速扫描。

最终构建正式 Windows NSIS，核对主程序、前端、VSTO/OLE 来自同一构建；安装后重新用真实 Word 空文档验收。交付逐项证据、失败恢复检查、必要的性能对比与安装包路径。报告里的43项检查只验证审查证据一致性，不是修复后的产品验收。

## 可直接发送给 agent 的一段话

请接手 `C:\Users\pojian_liao\Desktop\devspace\visualtex1.2.3-reference-3d207d7` 的 Windows 项目，基线 `3d207d7a81ff40014fd861b242cff96ea77060a2`，先完整阅读 `docs\audit-3d207d7\AUDIT_REPORT.md`、本交接页及对应 `evidence`。上一轮已完成用户八个原始活动文档的只读取证、全部主要真实 Word 操作链复现和公共根因审查，未修改产品实现。请按报告修复 OMML 多行宿主与单行编辑契约冲突、邻接公式身份/书签漂移及不完整回滚、批量编号物化与健康快速路径覆盖不全、MathType 引用别名与编号重写冲突、批量 OMML 字体上下文缺失、重绘对手动换行及已有正文的错误处理，以及 cases 空右定界符的语义往返误判；首次身份失效步骤及仅引用滞后的独立变体继续补证，不能把推测当结论。不得用删书签、强删边界、关指纹、吞异常或特定源码补丁掩盖问题。保护文档1、22、26、28、29、33、35、36这些未保存原件，不得在其上测试、保存或关闭；不得 reset/clean/stash/切分支/删未知文件/擅自提交或推送。修复后按报告完整矩阵，经真实安装版本、真实 Word 新建空文档和 Ribbon 对应处理链检验插入、编辑、关闭、编号、引用、转换、导入、重绘、失败恢复及性能，并构建正式 NSIS交付；测试程序通过、数量正确或元数据勾选均不能替代真实文档可用性验收。
