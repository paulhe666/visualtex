# 当前进度：按用户要求构建当前修改的 NSIS

后续指令：NSIS 交付后，用户明确要求“进行本地提交”。本地提交范围为当前整改代码、测试、审查/整改说明、真实 Word 证据和验收脚本；构建安装包、运行时备份及解包副本保留本地，不纳入 Git。此前“未提交”说明记录的是各阶段构建当时的状态。此次不推送，也不恢复安装或 Word 验收任务。

2026-09-06 最新任务范围：用户要求“先把现在已经修改的编译nsis，然后编译完成没问题之后给我结束对话”。因此停止后续 Word 操作、安装和完整矩阵，不再继续批量导入或修复。本轮只构建当前工作树、核验安装包并交付；不能把构建通过写成最终安装验收通过。

stage06 已完成并交付：桌面和 Office 前端、x64/x86 VSTO/OLE/MSI、主程序 Release、NSIS 均构建成功。项目静态检查、包内主程序嵌入前端校验及 37 项包内文件一致性检查全部通过。安装包 `apps/windows/src-tauri/target/release/bundle/nsis/VisualTeX_1.2.6_x64-setup.exe`，323777910 字节，SHA256 `A324ACB61252C15D9EC06FE118DA76A17339ABA6B30B94447421879413181F19`。详见 `NSIS_BUILD_STAGE06.md` 和 `evidence/stage06-*`。原始八份未保存 Word 文档未操作；没有执行安装、提交或发布。按用户要求结束本轮，不自动继续余下验收。

在用户改为构建任务之前已完成的补验：

- stage04ap 剩余转换全部完成，最终 6 OMath / 5 编号；三个格式实际编辑及关闭编辑器保护已记录。10 个正文段落（包含混合行内公式段落）的文字格式和段落属性在往返后全部相等，见 `stage04ap-final-prose-comparison.json`。字号和根号部分已经结束，不应重新当作待修改任务。
- stage05 三格式重绘分别完成普通回车和 Chr(11) 手动换行，共六个真实 Ribbon 用例；各有 2 公式和实际编号 1/2，正文顺序与源正文 XML 格式核验通过，六张结果截图已查看。见 `stage05-redraw-com-xml-review.json`。运行日志记录了耗时，但尚未完成性能基线对照。
- stage05 后续引用脚本在切换已记录测试文档时发生 COM“文件名无效”，操作尚未触发下一文档的引用；保留原失败记录，未用它冒充产品通过。用户随即要求构建并结束，本轮不再继续。

尚未完成的最终同源安装、安装后完整矩阵、完整性能对照，以及此前未证明消除的偶发问题，均随任务范围缩小而暂停。本次安装包不是“全部产品问题已解决”的声明。

以下是历史进度快照，保留时间顺序，不代表当前执行位置：

更新于 2026-09-06 21:34。HEAD 仍为 3d207d7a81ff40014fd861b242cff96ea77060a2，无提交、切分支或发布。NSIS 暂停。下方按时间保留证据，历史“待验证”不能当作当前未修改状态。

当前工作位置：04ap / Word77768 / 文档21，收口最后两处改动；不是重新排查已解决的字号、根号或整个格式互转功能。字号保留已经实现且有真实验证，04ap补充只改第二行12磅后转MathType内部Full=12、其余五式10.5，回OMML仍只有目标12磅。三格式关闭编辑器完整COM差异均0。首轮三格式往返后，包含混合行内公式段落在内的10个正文段落文本、普通文字格式和段落属性全部相等，见stage04ap-roundtrip-prose-comparison.json；旧04ao同一检查明确不相等，保留为负对照。

未关闭的产品问题是04am偶发行身份拒绝，不能把后续不复发冒称根因已证明。04ap的Ribbon隔离已实施；当前链继续执行剩余转换方向，最后一次MT→OMML尚待本轮结果。一次UI菜单未展开使脚本在转换启动前停止；已查看真实窗口，无产品错误框、无新增转换日志，仅继续未执行步骤，原始失败动作保留。

之后依次处理已有证据缺口和原审查未收口项，最后同源构建/安装后全矩阵/性能对照。已通过的独立功能不再列为待修，也不为每个诊断构建无条件重跑全矩阵。

已经实施的机制见 IMPLEMENTATION.md；已完成的局部验证不能合并冒充最终安装验收。

- 04al 修复 MathType 公共 MTEF 颜色定义。用户指出的现象是根号显示不完整：异常预览仍有根号几何记录，缺少 COLOR 1 对应的黑色定义。真实新 Word A/B、直接导入、编辑、回转和可见引用跳转已通过。432 项辅助测试通过。
- 04an，Word112840/文档12：从空白先插入一个左编号 MathType 和原引用，再批量导入完整六公式。7 OLE/6 MTPlaceRef，原式二进制与预览、原书签和原六个域均保持。原始 MT、转换后 OMML/VT 三次关闭编辑器的完整 COM 差异均为0。三种格式实际改第四式；六个有向转换均完成，最终7 OMath/6实际编号，两个原生 MathType 引用别名均保留。最终两次可见跳转分别高亮(0.0-1)、(0.0-3)，已查看截图。编号及引用均“+西文正文”10.5。一次较早的跳转截图与选区记录不一致，保留原证据，不算那一次通过。
- 04an 发现空行累积，整行因此仍未通过：第一个公式与正文间原有1个空白段落；三次转为OMML后分别2、3、4个。见 stage04an-growing-blank-evidence.json 及真实各步 COM/XML/截图。
- 04ao 公共修复：保留原后续段落的末尾 Range，创建表格后从原末尾重新解析段落，严格验证原文未变、唯一新增段落只有CR且无对象/域/框架/内容控件/书签，再移除这一新增段落。保留原空行和原书签；不再吞掉这一阶段的异常。当前 VSTO 哈希 55F87EA2A4B02F33A386CFFEAF03DA97096FC3A716A6E56CB3F3EC4DA7E1AFA1，实际新 Word67232。
- 04ao 文档15真实新空白→MT左编号+引用→OMML→MT→OMML。两次OMML均1公式/1编号/1引用、end156；完整COM仅4个新公式身份书签名不同，其他字段相同。原有空行保留，没有继续增加。完整批量及原后续内容的保护仍在验证。
- 04ao 辅助测试首次430通过、2项因沙箱拒绝产品temp目录而失败；这2项在所需权限下原样重跑通过，第一次失败日志和两份TRX均保留。
- 用户授权的清理：已不保存关闭完成根号对照的Word94656/文档8和Word71532/文档20。进程开始时间、对象/域/书签数量与范围均先核验；其他文档和原始8份文档未关闭。

未完成且仍在继续：

1. 04am多轮转换末尾的OMML行归属偶发拒绝。外部完整COM确认原生失败恢复差异0，实际转换根因仍未消除。04an只补了逐项诊断和恢复前原始XML记录，不能称已修；04an六方向未复发不等于解决。
2. 当前04ao正在原始OMML已有编号的完整导入/编辑/互转分支；其余三格式×空白/已有编号完整矩阵还需在最终候选重新收口。
3. 新空行修复的完整批量、尾部和邻接保护；混合正文格式、所有非目标对象与书签、字体/字号正向编辑、CR/手动换行三种重绘、保存重开、性能对照。
4. 主程序/前端/x64和x86 VSTO/OLE同源构建，新NSIS，无损安装及安装后完整矩阵。运行中的OLE服务器仍为旧已安装版本，不能称同源。

原始8份文档最近完整保护证据为 stage04al-original-preservation-comparison.json：与04a完整COM一致，全部未保存；后续操作均使用记录的独立测试Word进程。

21:23补充：04ao Word67232新文档20（不是已关闭的Word71532旧文档20）完成原始OMML已有编号分支的六方向转换，7公式/6编号均保留。导入前后原式COM、原表格、原三个域、所有原书签及原式OMML XML完全相同；OMML/MT/VT三次关闭完整COM差异0，三格式均实际改第四式。MT中连续编号与按节编号来回重写成功，原VTEqNum兼容引用分别从1/3更新至0.0-1/0.0-3。最终引用实际双击高亮0.0-3，截图已查看。三次OMML状态首式至正文的原空行数相同，见stage04ao-stable-blank-evidence.json。9个普通正文段落XML格式完全相同，但混合行内正文多了snapToGrid=0，故这一分支仍不记整行通过。

04ap待真实验收的公共修复：ThisAddIn的写入保护现在覆盖字号Ribbon同步取值、SelectionChange中的宿主预读及排队的旧回调；写入结束后在Word调度器刷新一次控件。新增日志只记录被延后的读取次数，不跳过公式完成后的身份/语义检查。这处代码确有隔离遗漏，但尚不能据此认定04am偶发拒绝根因已消除。

04ap同时移除StabilizeInlineOmmlFractionLineGrid及两个入口调用，OMML的字体/字号处理不再改写正文段落的网格对齐。数学字形仍走原生OMML排版；既有网格样式保留。当前实际候选哈希6861688D7AECF188B7A7B4C79DABBFBE6D35763FD54ECCBDC8C37025F58054AC，Word77768新文档21正在完整OMML空白分支，并补仅改字号的真实编辑/Ribbon验证。04ap构建通过；尚未构建NSIS。
